# Synk API Specification

## Overview

The Synk backend is built with FastAPI.

REST APIs are used for device management and pairing.


WebSocket is used for real-time clipboard synchronization.

Base URL:

```text
/api/v1
